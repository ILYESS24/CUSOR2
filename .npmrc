# Configuration npm pour éviter la compilation des modules natifs non nécessaires pour le web
# Cela évite les erreurs de compilation pour native-keymap et autres modules Electron-only
ignore-scripts=true
