# Configuration npm pour le build
# On n'ignore pas tous les scripts car certains sont nécessaires
# Mais on empêche la compilation de native-keymap en l'excluant du postinstall

# Valeurs stub pour Electron (non utilisées en mode web, mais nécessaires pour le build)
target="32.2.6"
ms_build_id="20241202T135610Z"
